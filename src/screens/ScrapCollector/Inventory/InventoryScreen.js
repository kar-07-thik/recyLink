import { Pressable, Text, View } from 'react-native';
import { mockInventory } from '../../../api/scrapCollector';
import { Screen, SectionTitle } from '../../../components/Screen';
import { StatCard } from '../../../components/StatCard';
import { StatusBadge } from '../../../components/StatusBadge';
export default function InventoryScreen() { return <Screen title="Inventory"><View className="-mx-5 mb-4 flex-row pl-5"><StatCard label="Total quantity" value="27" /><StatCard label="Total weight" value="42 kg" /><StatCard label="Item count" value="18" /><StatCard label="Ready for recycler" value="11" /></View><View className="mb-4 flex-row flex-wrap">{['All', 'Electronics', 'Batteries', 'Plastics', 'Metals', 'Cables', 'Other'].map((filter, index) => <Pressable key={filter} className={`mr-2 mb-2 rounded-full px-3 py-2 ${index === 0 ? 'bg-[#2E7D32]' : 'bg-white'}`}><Text className={`text-xs font-bold ${index === 0 ? 'text-white' : 'text-[#526052]'}`}>{filter}</Text></Pressable>)}</View><SectionTitle action="AI Scan Records">Your materials</SectionTitle>{mockInventory.map((item) => <View key={item.name} className="mb-3 rounded-2xl bg-white p-4"><View className="flex-row justify-between"><View className="flex-1"><Text className="text-lg font-bold text-black">{item.name}</Text><Text className="mt-1 text-sm text-[#526052]">{item.category} · {item.quantity} · {item.weight}</Text></View><Text className="font-bold text-[#2E7D32]">{item.value}</Text></View><Text className="mt-2 text-xs text-[#526052]">Added {item.date}</Text><View className="mt-3 flex-row"><StatusBadge status={item.recyclable ? 'Completed' : 'Pending'} /><View className="ml-2"><StatusBadge status={item.status === 'Ready for recycler' ? 'Accepted' : 'Pending'} /></View></View></View>)}<SectionTitle>Inventory history</SectionTitle><Text className="text-sm leading-6 text-[#526052]">20 Sep · Added Dell Latitude 5420{ '\n' }18 Sep · Updated copper wire bundle{ '\n' }14 Sep · Transferred batteries to handling queue</Text></Screen>; }
