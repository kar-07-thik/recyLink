import { Pressable, Text, View } from 'react-native';
import { Screen, SectionTitle } from '../../../components/Screen';
import { StatusBadge } from '../../../components/StatusBadge';
import { StatusTimeline } from '../../../components/StatusTimeline';
const pickups = [{ customer: 'Anita Sharma', location: 'Indiranagar', type: 'Laptop and cables', quantity: '4 items', date: 'Today, 4:30 PM', status: 'Accepted' }, { customer: 'Green Office Hub', location: 'Koramangala', type: 'Mixed electronics', quantity: '12 kg', date: 'Tomorrow, 10:00 AM', status: 'Completed' }];
export default function MyPickupsScreen() { return <Screen title="My Pickups"><View className="mb-5 flex-row rounded-2xl bg-white p-1">{['Upcoming', 'Active', 'Completed', 'Cancelled'].map((tab, index) => <View key={tab} className={`flex-1 rounded-xl p-2 ${index === 0 ? 'bg-[#2E7D32]' : ''}`}><Text className={`text-center text-xs font-bold ${index === 0 ? 'text-white' : 'text-[#526052]'}`}>{tab}</Text></View>)}</View>{pickups.map((pickup) => <View key={pickup.customer} className="mb-4 rounded-3xl bg-white p-5"><View className="flex-row justify-between"><Text className="text-lg font-bold text-black">{pickup.customer}</Text><StatusBadge status={pickup.status} /></View><Text className="mt-3 text-sm text-[#526052]">⌖ {pickup.location} · {pickup.type}</Text><Text className="mt-1 text-sm text-[#526052]">{pickup.quantity} · {pickup.date}</Text><View className="mt-4"><StatusTimeline current={pickup.status === 'Completed' ? 5 : 2} /></View><View className="mt-4 flex-row flex-wrap"><Pressable className="mr-2 mb-2 rounded-xl bg-[#2E7D32] px-3 py-3"><Text className="font-bold text-white">Track Status</Text></Pressable><Pressable className="mr-2 mb-2 rounded-xl border border-[#D8E8D8] px-3 py-3"><Text className="font-bold text-black">Navigate</Text></Pressable><Pressable className="mb-2 rounded-xl border border-[#D8E8D8] px-3 py-3"><Text className="font-bold text-black">Contact</Text></Pressable></View></View>)}</Screen>; }
