import { Pressable, Text, View } from 'react-native';
import { mockRequests } from '../../../api/scrapCollector';
import { Screen, SectionTitle } from '../../../components/Screen';
import { StatusBadge } from '../../../components/StatusBadge';

export default function PickupRequestsScreen({ navigation }) { return <Screen title="Pickup Requests"><SectionTitle action="12 open">Nearby requests</SectionTitle>{mockRequests.map((request) => <View key={request.id} className="mb-4 rounded-3xl border border-[#D8E8D8] bg-white p-5"><View className="flex-row justify-between"><View><Text className="text-lg font-bold text-black">{request.customer}</Text><Text className="mt-1 text-xs text-[#526052]">{request.id} · {request.date}</Text></View><StatusBadge status={request.status} /></View><Text className="mt-4 text-sm text-black">{request.type} · {request.quantity}</Text><Text className="mt-1 text-sm text-[#526052]">⌖ {request.location}</Text><Text className="mt-1 text-sm text-[#526052]">☎ {request.phone}</Text><View className="mt-4 flex-row"><Pressable onPress={() => {}} className="mr-2 flex-1 rounded-xl bg-[#2E7D32] p-3"><Text className="text-center font-bold text-white">Accept</Text></Pressable><Pressable onPress={() => {}} className="mr-2 rounded-xl border border-[#A92B2B] px-4 py-3"><Text className="font-bold text-[#A92B2B]">Reject</Text></Pressable><Pressable onPress={() => navigation.navigate('RequestDetails', { request })} className="rounded-xl border border-[#D8E8D8] px-3 py-3"><Text className="font-bold text-black">Details</Text></Pressable></View></View>)}</Screen>; }
